services:
  web-admin:
    build:
      context: .
      dockerfile: apps/web-admin/Dockerfile
    image: noogym-web-admin:latest
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
